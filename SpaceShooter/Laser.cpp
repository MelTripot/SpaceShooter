#include "Laser.hpp"

using namespace std;

// Laser::Laser(){}

Laser::Laser(const float _angle): angle(_angle) 
{
	// Ajouter gestion visuel
}

Laser::~Laser(){}

Laser::LaserUpdate()
{
	// ce que fait le laser à chaque frame
	/*
		radient = ((r+270)*M_PI/180);
		// On peut changer la valeur (0.1) pour chager la vitesse
		xa = 1.5* cos(radient);
		ya = 1.5* sin(radient);
		exist = true;
	*/
}